TO CHANGE FROM PLAIN PYTHON TO CODE THAT WORKS WITH DISCORD:


🟨 NEST EVERYTHING EXCEPT IMPORTS IN `async def main(message, client):`


🟨 AT THE END, ADD:
if __name__ == "__main__":
  main()


🟨 MAKE ALL FUNCTION DEFINITIONS ASYNC; MAKE ALL FUNCTION CALLS AWAIT


🟨 REPLACE `print()` WITH `await message.channel.send`


🟨 REPLACE `input("prompt")` WITH:
def check(msg):
  return msg.author == message.author

await message.channel.send("prompt")
input_var = await client.wait_for('message', check=check, timeout=##)
input_var = input_var.content