# create a dict of birthdays and somehow make private

elif firstword == "birthdays" or firstword == "bdays":
      if len(cmd.split()) == 1:
        bdays_list = ""
        for name in bdays:
          bdays_list += name + ": " + bdays[name] + "\n"
        await message.channel.send(bdays_list)
      else:
        secondword = cmd.split()[1]
        if secondword == "add":
          if len(cmd.split()) == 3:
            bdays[cmd.split()[2]] = cmd.split()[3]
        if secondword in bdays:
          await message.channel.send(secondword + "'s birthday: " + bdays[secondword])